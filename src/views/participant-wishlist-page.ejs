<%- include('header') %>
    <div class="my-4">
        <a href="/manage-event/<%= eventId %>" class="text-teal-500 hover:text-teal-700 ml-2"> <i
                class="fa-solid fa-arrow-left-long"></i> Go back to manage event</a>
    </div>
    <div class="p-6">
        <div class="responsive-text-xl mb-4">All Members wishlist</div>
        <div>
            <% for(let i of list) { %>
                <div class="responsive-text-md font-bold mb-2">
                    <%= i.name %>
                </div>
                <% for(let j of i.wishlist) { %>
                    <ul>
                        <li class="text-teal-500">
                            <%= j %>
                        </li>
                    </ul>

                    <% } %>

                        <% if(!i.wishlist?.length) {%>
                            <span>---</span>
                            <% } %>

                                <% } %>
        </div>
    </div>


    <%- include('footer') %>